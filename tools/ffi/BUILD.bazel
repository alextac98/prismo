load("@prismo_crates//:defs.bzl", "all_crate_deps")
load("@rules_rs//rs:rust_binary.bzl", "rust_binary")

rust_binary(
    name = "diplomat_codegen_tool",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "prismo_ffi_tools",
    edition = "2024",
    visibility = ["//visibility:public"],
    deps = all_crate_deps(normal = True),
)
