load("@crates//:defs.bzl", "all_crate_deps")
load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "example_rust",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "prismo_example_rust",
    edition = "2024",
    deps = all_crate_deps(normal = True) + [
        "//crates/core:core",
    ],
    visibility = ["//visibility:public"],
)
